# Gemini command: engine-maintainer

Read `core/instructions/engine-maintainer.md`. Inspect first. Only an explicit
approval permits `git pull --ff-only`; dirty or diverged trees are skipped or
blocked.
