# Skills Kaizen execution log — 26 September 2026

This log records accepted work from the Skills Kaizen plan. Each phase stays conditional on its value gate. Planned work, unavailable evidence and unaccepted pilots remain distinct from completed work.

## P00 — Governance and model-policy drift

**Decision: repair the stale guidance; retain the current Codex policy.** The controlling user-level policy sets GPT-6 Luna/high as the default for orchestration, review and execution, and permits Astra only when Peter explicitly selects it. The plan's proposed Astra planning/review assignment does not override that live instruction. No engine policy helper or host setting was run in apply mode.

**Baseline and problem.** The Digital Research currentness gate named an old `skills-web-dev` path and a portfolio-wide register file that does not exist in the canonical `digital-research-engine` checkout. The coordinator README described an ECC-inspired plugin profile as vendor schema requirements, including a mandatory version and prohibited `agents`/`hooks` fields. Both claims could direct maintainers to missing or incorrect requirements. The Research and Dev checkouts already contained uncommitted 26 September GPT-6 policy updates when this work began; those changes were preserved and excluded from this phase's commit.

**Smallest change and expected value.** Correct the currentness route and state that project evidence registers carry claims where no maintained portfolio register exists. Clarify that the coordinator's generator uses a strict local profile and that Claude Code's own current manifest reference controls vendor requirements. This prevents maintainers from using a dead path or confusing house conventions with vendor requirements.

**Model currency review (checked 2026-09-26).** OpenAI's [Models documentation](https://developers.openai.com/api/docs/models) identifies GPT-6 Astra as its flagship for complex reasoning and coding and GPT-6 Luna for cost-sensitive, high-volume workloads. The [Astra model page](https://developers.openai.com/api/docs/models/gpt-6-astra) lists `gpt-6-astra` and reasoning efforts from low through max; the [Luna model page](https://developers.openai.com/api/docs/models/gpt-6-luna) lists `gpt-6-luna` and its API usage. The [API pricing table](https://developers.openai.com/api/docs/pricing) gives standard short-context rates per million tokens of $10 input / $50 output for Astra and $0.10 / $0.50 for Luna. These are API rates, not Peter's subscription bill. Official sources support the model IDs and a broad task-fit distinction; no paired local quality or latency test was run. The installed Codex catalogue cache listed Astra and Luna; the configured model was Luna/high. Cache presence does not establish account entitlement or the model served in an already-running session, so account entitlement is **NOT_ASSESSED**. **Retain** the user-authorised Luna/high default; no evidence or authority supports changing it.

**Plugin-source correction (checked 2026-09-26).** The [Claude Code plugin manifest reference](https://code.claude.com/docs/en/plugins-reference) says the manifest is optional, `name` is the only required field, `agents` and `hooks` are supported, and strict validation can be requested with `--strict`. The coordinator's generated explicit-path-array manifest remains a local profile. Installed-host behavior was not exercised in this phase.

**Files changed.** `digital-research-engine/docs/continuous-improvement/kaizen-currentness-gate.md`; `chwezi-engine-agents/README.md`; this execution log.

**Acceptance evidence.** Canonical engine roots and relevant source files were checked; the referenced central register is absent while `digital-research-engine/docs/source-registers/` contains only domain-specific registers. The new wording states this limitation. Official model and plugin pages were opened and checked. No active policy setting was changed. On 2026-09-26, `git diff --check` passed in the coordinator and Research repositories; the coordinator catalogue contract validator passed; Research `validate_source_currency.py tests/fixtures/source-currency.json` passed its metadata/date checks (the command explicitly leaves claim support unassessed); and Dev `pytest tests/test_codex_model_policy.py -q` passed 16 tests. The tests use temporary Codex homes. Claude Code's `claude plugin validate` was not run, so installed-host manifest acceptance is **NOT_ASSESSED**.

**Value and rollback.** Value measure: the gate points only to existing evidence locations and the README distinguishes vendor requirements from house choices. Both documentation-only edits can be reverted independently if newer first-party documentation or an approved central register changes these findings.

**Open items.** Runtime/account entitlement and installed Claude Code plugin validation remain **NOT_ASSESSED**. The model-policy evidence does not establish task-specific quality or latency. No independent reviewer was assigned; independent review remains **NOT_ASSESSED**.

## P01 — Reproducible portfolio baseline

**Decision: repair the reproducibility gap and keep the existing inventory.** The saved audit gave raw and active counts but did not pin the contents of the nine uncommitted files in each public engine checkout. The smallest useful extension was a read-only inventory script plus a snapshot that hashes dirty working files while keeping the prior router/native-validator classification separate.

**Baseline and value.** At capture, all eleven public engine checkouts were on `main`, each had nine uncommitted modified policy files, and no staged changes were present. The new snapshot records each HEAD, remote, branch, changed path, working-file hash and tracked diff hash. The repeatable raw scan found 1,174 public `SKILL.md` files; the same-day prior router/native evidence found 1,167 reachable entries. Capturing the modified file contents makes later policy integration review reproducible. A no-change option would preserve only the less specific dirty-path counts. Maintenance cost is one read-only script; it does not modify or normalize engine repositories.

**Artifacts.** `docs/operations/skills-kaizen-evidence/2026-09-26/` contains `baseline.json`, `skill-inventory.jsonl`, `coordination-skill-inventory.jsonl`, `count-reconciliation.md`, `runtime-composition.json` and `validation-manifest.json`. The full per-user plugin list stays in local-only evidence. The scanner is `scripts/kaizen_portfolio_snapshot.py`.

**Reproducibility.** Before public-scope cleanup, the snapshot command was run twice with unchanged inputs; both runs matched. The published `baseline.json` SHA-256 is `f9f27da6605d9f83708be7cf991f3d9ed6df5045c7f1a545697d0aea169d0cf0`; `skill-inventory.jsonl` is `f8e03cee65660ec48f9627878b2caaff15d4c092879cc93918ad73a729d0adc9`; and `validation-manifest.json` is `dbe23600587793f0d53379a52cc164b39f97a90dab478933b911afcd088561c4`. The detailed validator output was omitted from the public manifest for one registry check; its original log digest remains recorded. JSON parsing passed.

**Runtime and count findings.** Read-only host commands found 14 installed/enabled Claude Code plugins and 30 Codex plugins (21 enabled, 9 disabled), with no Chwezi domain-engine plugins enabled in either runtime. The detailed user-profile names and versions remain local-only. The coordinator is counted separately (three raw skills). Static plugin manifests have 1,160 entries against 1,167 reachable skills; the existing audit identifies nine Business Plan entries and two Chwezi Dev entries not represented as exact manifest paths. Installed-host behavior was not tested.

**Validation evidence.** The same-day prior engine check manifest has 37 public/coordinator checks: 34 executed-pass, two executed-fail-or-unavailable and one NOT_ASSESSED. The two Linux findings are the three reported network-equipment skill-contract failures (one validator result) and unavailable WSL distribution (one environment result); Design has no source-ingestion guard check. These prior checks were not rerun because P01 changed no engine skill files. Coordinator catalogue validation and P00's direct checks passed as recorded above.

**Limits and acceptance.** The fresh scan assigns every discovered public skill file a path, content hash, byte/line count, frontmatter name and raw/excluded discovery class. It explicitly does not assign per-file router reachability, alias/generated-copy semantics or cross-host shadowing. Existing active totals remain supported by same-day router/native-validator evidence, but the semantic inventory, 10–20-case sampling frame per engine and installed-host runtime validation are **NOT_ASSESSED**. P01 is implemented as a reproducible baseline, with those acceptance items open; no retirement or count-driven edit was made. Changes can be rolled back by removing only the new snapshot script and its evidence folder.

**P01 completeness correction.** Review against the original raw inventory showed that its 1,177 rows comprise 1,174 domain files plus three coordinator files. The first committed JSONL held only the 1,174 domain files. Added a separate three-row `coordination-skill-inventory.jsonl`; its path/hash pairs match the original same-day raw scan. The domain count and frozen baseline are unchanged. The supplemental inventory SHA-256 is `a6dd2d1b51c4a9a36aa92032e19bc206e02bc1c282e077767251d778a605b701`; the updated scanner SHA-256 is `a4a012e81c30b97b7267deab08345ba340ba986ca1f3635142f50d12e6c7a5d6`.

## P02 — Claim currency and source register

**Decision: reuse the existing Research validators and register only the volatile claims changed in P00.** The observed currentness defect was a dead source-register path and an unsupported plugin-schema description. The currentness and manifest claims also include volatile model IDs, task positioning and API prices. A full regex sweep of every engine would create a large unreviewed queue; the smaller useful change is a dated, narrow register for the claims used by P00, with reverse links to the affected artifacts.

**Artifact.** Digital Research now contains `docs/source-registers/skills-kaizen-wave2-2026-09.json` and the matching source-verifier report. Five claim records point to official OpenAI model/pricing pages and the official Claude Code plugin reference. Each includes a source locator, scope, review date, support state, uncertainty, reviewer status, and affected artifact paths. The separate local runtime catalogue observation remains in local-only evidence.

**Verification (2026-09-26).** `python -B -X utf8 scripts/validate_source_currency.py docs/source-registers/skills-kaizen-wave2-2026-09.json --as-of 2026-09-26` reported zero metadata/date findings; its PASS does not assess claim support. `python -B -X utf8 tools/verification/source_verifier.py docs/source-registers/skills-kaizen-wave2-2026-09.json --no-archive --format json --out docs/source-registers/skills-kaizen-wave2-2026-09-verification.json` checked all five URLs (5 pass, 0 fail) and recorded five warnings because the structural tool does not certify semantic support; it correctly returned exit 2 with `release_ready: false`. Archive lookup remains NOT_ASSESSED. `python -B -X utf8 -m pytest tests/test_source_currency.py -q -p no:cacheprovider` passed 93 tests covering source IDs, expiry, future dates, scope, source linkage, malformed inputs and read-only behavior. `git diff --check` passed.

**Limits and value.** This register covers only the five external claims used in P00; it is not an eleven-engine currentness census and does not replace project-specific evidence registers. OpenAI API prices are not subscription costs. No local quality/latency comparison was run, and individual account entitlement, archive presence, installed host plugin behavior and independent human review remain **NOT_ASSESSED**. The no-change option would leave the first repairs without a refresh date or reverse impact links. This bounded register uses the existing validation stack, with no new schema or runtime dependency; each source is due for recheck by 2026-10-03. Revert only the two register files if a source entry is wrong.
